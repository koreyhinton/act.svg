import * as assert from "uvu/assert";
import "../js/node-vertex.js"

(async()=>{await Promise.all([

    /*[
        { t: 0, inj: null, expect: 1 }, // test
        { t: 0, inj: {x:0, y:0}, expect: 0 }, // reverse test
    ].testEach("Placeholder test 0", async ({t,inj,expect,scope}) => {
        // assert.ok(true);
        let vtx = { x: 0, y: 0 };
        if (t == 0) {
        for (var i=0; i<100000; i++){
            for (var j=0; j<100; j++){}
        } }
        let iVtx = window.vxInverse.call(scope, vtx);
        assert.is(iVtx.x, expect);
        assert.is(iVtx.y, expect);
    }), // end

    [{ t: 1, inj: null }].testEach("Placeholder test 1", async () => { 
        assert.ok(true); 
    }),    

    [{ t: 2, inj: null }].testEach("Placeholder test 2", async () => { assert.ok(true); }),
*/

])})();
